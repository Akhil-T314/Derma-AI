const { Pool } = require("pg"); require("dotenv").config({path: "../backend/.env"}); const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false} }); pool.query("SELECT to_char(created_at, $$Mon DD HH12:MI AM$$) as month_label, confidence_score as risk_score FROM Scans WHERE patient_id = $1 ORDER BY created_at ASC", ["8822ddfb-9edd-433d-96ac-13da36100dee"]).then(r => console.log(JSON.stringify(r.rows, null, 2))).catch(console.error).finally(()=>pool.end());
